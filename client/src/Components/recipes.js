import React from 'react';
import RecipeContainer from '../containers/recipes'

const Recipes =() => {
    return(
        <div>
        <RecipeContainer/>
        </div>
    );
};

export default Recipes;